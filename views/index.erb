<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.2/css/bootstrap.min.css" integrity="sha384-PsH8R72JQ3SOdhVi3uxftmaW6Vc51MKb0q5P2rRUpPvrszuE4W1povHYgTpBfshb" crossorigin="anonymous">

  <link rel="stylesheet" type="text/css" href="app.css">

  <title>Mastermind</title>
</head>
<body>
  <div class="container">
    <div class="col-lg-6 offset-lg-3">
      <div class="jumbotron">

        <h1>Mastermind</h1>
        <p class="lead">
          Instructions goes here...
        </p>
        <hr>
        <p>
          Content goes here.
        </p>
        <form>
          <div class="row">

            <% 4.times do |i| %>
              <div class="col">
                <label for=<%= "'color#{ i + 1 }'" %>><%= i + 1 %>. peg</label>
                <select multiple class="form-control" name=<%= "color#{i + 1}" %> required>
                  <option class="red" value="1"></option>
                  <option class="blue" value="2"></option>
                  <option class="green" value="3"></option>
                  <option class="yellow" value="4"></option>
                  <option class="orange" value="5"></option>
                  <option class="purple" value="6"></option>
                </select>
              </div>
            <% end %>
          </div>
          <br>
          <input type="submit" id="submit" value="Guess!">
        </form>
        <hr>

        <table class="table table-bordered table-bordered table-sm">
          <caption>List of guesses</caption>
          <thead>
            <tr>
              <th>#</th>
              <th>Guess</th>
              <th>Evaluation</th>
            </tr>
          </thead>
          <tbody>
            <% feedback.each_with_index do |round, idx| %>
              <tr<%= " class='table-success'" if guess[idx] == code %><%= " class='table-danger'" if guess[idx] != code && guess_count == 8 %>>
                <th scope="row"><%= idx + 1 %></th>
                <td><%= guess[idx].map { |num| "<div class='peg #{num.to_color}'></div>" }.join(" ") %></td>
                <td><%= round.map { |num| "<div class='peg #{num.to_color}'></div>" }.join(" ") %></td>
              </tr>
            <% end %>
          </tbody>
        </table>

        <p>
          <b>Code:</b> <%= code %>
        </p>

        <p>
          <b>Message:</b> <%= message unless message.nil? %>
        </p>

        <a class="button" href="javascript:history.go(0)">Play again</a>

      </div>
    </div>
  </div>

</body>
</html>